# Copy this file to .streamlit/secrets.toml for local use
# On Streamlit Community Cloud, paste these values into the Secrets manager
# (App settings → Secrets) — do NOT commit secrets.toml to git.

NVIDIA_API_KEY = "nvapi-your-key-here"
DB_PATH = "data/sample.db"
